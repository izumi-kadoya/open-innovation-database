<%= link_to "csv upload", new_record_path %>

<div class="index-body">
  <div class="industry-list">
   <div class="industry-list-upper">
     industries
   </div>
  <ul>
    <% ["Life and Health Insurance", "Non-life Insurance", "Financial Services", "Logistics", "Pharmaceuticals", "Retail"].each do |industry| %>
      <li><%= link_to industry, filter_by_industry_records_path(industry: industry) %></li>
    <% end %>
  </ul>
  </div>



  <div class="company-list">
    <div class="company-list-upper">
     <%= link_to "name 🔼", records_path(order_by: 'company_name')  %>
     <%= link_to "upload date 🔽", records_path(order_by: 'created_at')  %>
    </div>

    <ul>
     <% @companies.each do |company_name, id| %>
     <li><%= link_to company_name, record_path(id) %></li>
     <% end %>
    </ul>
  <div>

</div>